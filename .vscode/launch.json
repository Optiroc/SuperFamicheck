{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/superfamicheck",
            "args": ["test/data/rom1.sfc"],
            "preLaunchTask": "${defaultBuildTask}"
        }
    ]
}
